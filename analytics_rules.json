{
  "rules": {
    ".read": false,
    "hourly": {
      ".read": true,
      "$hour": {
        ".write": true,
        ".validate": "newData.hasChildren(['swipes']) && newData.child('swipes').isNumber() && newData.child('swipes').val() >= 0"
      }
    }
  }
}
